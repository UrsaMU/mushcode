/**
 * @module
 * RhostMUSH platform plugin — re-exported from `@ursamu/mushcode-rhost`.
 *
 * The implementation now lives in its own standalone package.
 * Import directly from `@ursamu/mushcode-rhost` or via `@ursamu/mushcode/rhost`.
 */
export * from "@ursamu/mushcode-rhost";
